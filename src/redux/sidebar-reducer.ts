let initialState = {};
type InitialState = typeof initialState;
type ActionType = {};

const sidebarReducer = (state: InitialState = initialState, action: ActionType) => { return state; };

export default sidebarReducer;