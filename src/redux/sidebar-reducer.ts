let initialState = {};
type SidebarInitialState = typeof initialState;
type ActionType = {};

const sidebarReducer = (state: SidebarInitialState = initialState, action: ActionType) => { return state; };

export default sidebarReducer;