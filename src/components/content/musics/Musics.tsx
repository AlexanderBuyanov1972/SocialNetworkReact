import React from 'react';
import styles from './Musics.module.css';

type PropsType = {}

const Musics: React.FC<PropsType> = (props: PropsType) => {
    return (
        <div>
        Musics
         </div>
    );
}


export default Musics;